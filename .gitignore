# — Node / package managers —
node_modules/
.pnpm-store/
.npm/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# — Build / caches —
dist/
build/
.vite/
.cache/
.eslintcache
coverage/

# — Env/secrets (NUNCA commit) —
.env
.env.*
!.env.example    # mantenha um exemplo sem segredos

# — SO / editores —
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# — Logs —
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# — Misc —
*.local

